import { Button } from '@mantine/core';

function TeamDetailsBtn() {

    return (
        <>
            <Button px={15} color="rgb(249, 203, 0)" variant="outline" radius={7} fz={16} style={{ borderWidth: 3 }} >Arbeit Team 1</Button>
        </>
    );
}

export default TeamDetailsBtn;